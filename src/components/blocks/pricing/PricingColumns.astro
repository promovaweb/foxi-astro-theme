---
// Seção de Planos de Preços
// ------------
// Descrição: Uma seção com uma lista de 3 planos de preços com alternância para alternar entre preços mensais e anuais.

// Componentes
// - Layout
import Section from '@components/ui/Section.astro'
import Row from '@components/ui/Row.astro'
import Col from '@components/ui/Col.astro'
// - UI
import PricingTable from '@components/ui/pricing-tables/PricingTable.astro'
import Toggle from '@components/ui/forms/form-elements/Toggle.astro'

// Conteúdo
import pricingTablesData from '@data/json-files/pricingTablesdata.json'

// Script
import PricingChange from '@components/scripts/PricingPlanChange.astro'

// Props
type PricingTableData = {
	header: {
		title: string
		subtitle: string
		currency: string
		price: string
		priceLabel: string
		priceMonthly?: string
		priceLabelMonthly?: string
		buttonName: string
		buttonLink: string
	}
	body?: {
		listItems: Array<{ listItem: string }>
	}
	footer?: {
		buttonName: string
		buttonLink: string
	}
	type: 'basic' | 'featured'
}

const pricingData: PricingTableData[] = pricingTablesData as PricingTableData[]
---

<Section>
	<Row>
		<Col span="12" align="center" classes="flex items-center justify-center gap-4 mb-12">
			<span id="monthlyLabel" class="font-semibold text-neutral-400 transition-colors"
				>Cobrança mensal</span
			>
			<Toggle id="pricingType" label="pricing" showLabel={false} checked={true} />
			<span
				class="font-semibold text-neutral-400 text-primary-500 transition-colors"
				id="annualLabel">Cobrança anual</span
			>
		</Col>
		{
			pricingData.map((item, key) => (
				<Col span="4">
					<PricingTable data={item} listType="checklist" id={`plan-${key}`} />
				</Col>
			))
		}
	</Row>
</Section>
<PricingChange />
